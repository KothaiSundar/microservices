server:
  port: 8080
spring:
  application:
    name: "accounts"
  #cloud:
   #stream:
    #bindings:
     #springCloudBusInput:
      #group: accounts
  profiles:
    active: "prod"
  datasource:
    url: jdbc:mysql://localhost:3307/accountsdb
    username: root
    password: root
  jpa:
    show-sql: true
  sql:
    init:
      mode: always
  config:
    import: "optional:configserver:http://localhost:8071/"

management:
  endpoints:
    web:
      exposure:
        include: "*"
  
    
